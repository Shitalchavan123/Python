def Add(*values):
    sum=0
    for no in values:
        sum=sum+no
    return sum

def AddX(*values)
    for i in range(0,len(values),1)
        sum=sum+no
        return sum

def main():


   Ans=Add(1,2,3,4,5)
   print("Addition=",Ans)

if __name__ == "__main__":
     main()